/**
cuando se hace titleCase de un string,
esta lista de palabras se convierte a mayuscula

- la key es la palabra, TIENE q estar en minuscula
- el value es como se muestra la palabra en la interfaz grafica */
const specialWords: { [key: string]: string } = {
  cc: "CC",
};

export default specialWords;
