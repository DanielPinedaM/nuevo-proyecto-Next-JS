import IObjSessionStorage from '../interface/interface-session-storage';

// nombres de las propiedades q se guardar en SessionStorage
export const objSessionStorage: IObjSessionStorage = {
  token: 'token',
  username: 'username',
};
